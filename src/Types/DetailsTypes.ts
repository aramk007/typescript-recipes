export type detailsType = {
  name: string,
  image: string,
  caloriesPerServing: number,
  cuisine: string,
  ingredients: [string],
  mealType: [string],
  instructions: [string],
  prepTimeMinutes: number,
}